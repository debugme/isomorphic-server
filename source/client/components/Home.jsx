import React, { Fragment } from 'react'

const Home = () => (
  <Fragment>
    <div>HOME</div>
    <button onClick={() => console.log('clicked...')}>Click Me</button>
  </Fragment>
)

export default Home